# BTC 미래가격 예측봇 성능향상 22가지 개선 포인트

## 🔴 최상위 우선순위 (구조적 한계 해결)

---

### 1. 장기 예측 구간의 근본적 불확실성 처리

**현재 문제:** 90일, 180일, 365일 예측을 1일 예측과 동일한 구조(점 추정)로 처리한다. 180일 보정 로직이 별도로 있다는 것 자체가 모델이 장기를 못 맞힌다는 증거다.

**개선안:**
- 점 추정 → **확률 분포 예측**으로 전환 (Quantile Regression 또는 Mixture Density Network)
- 기간별로 **예측 신뢰구간 폭**을 명시적으로 출력
- 180일 수동 보정 로직 제거 → 모델 자체가 분포로 불확실성을 표현

**기대 효과:** 사용자에게 "85,000~120,000달러 (80% 신뢰구간)" 같은 실용적 정보 제공, 과신 방지

---

### 2. 학습/검증 데이터 분할 전략 재설계

**현재 문제:** 시계열 데이터에서 어떤 분할 전략을 쓰는지 명시가 없다. 일반 랜덤 분할이면 미래 정보 누수(data leakage)가 발생한다.

**개선안:**
```
Expanding Window 또는 Walk-Forward 검증
├── Train: 2015-01-01 ~ 2021-12-31
│   Valid: 2022-01-01 ~ 2022-06-30
│   Test:  2022-07-01 ~ 2022-12-31
├── Train: 2015-01-01 ~ 2022-06-30
│   Valid: 2022-07-01 ~ 2022-12-31
│   Test:  2023-01-01 ~ 2023-06-30
└── ... 반복
```
- Purged K-Fold (embargo 구간 포함) 적용
- 각 기간 모델(1d, 7d, 30d, ...)마다 해당 horizon 이상의 embargo gap 확보

**기대 효과:** 과적합 탐지 정확도 대폭 향상, 실전 성능 추정의 신뢰성 확보

---

### 3. Target 설계 개선 — 순수 로그수익률의 한계

**현재 문제:** `target_log_return = log(미래가격/현재가격)`은 변동성 체제(regime)를 무시한다. 2020년 코로나 때 -30%와 2024년 횡보장에서 -3%는 모델에게 완전히 다른 의미인데 같은 스케일로 학습된다.

**개선안:**
- **변동성 정규화 수익률:** `target = log_return / realized_vol_Nd`
- 또는 **Sharpe-like target:** 기간 수익률을 해당 기간 변동성으로 나눈 값
- 장기 구간은 **누적 수익률 대신 연환산 수익률**로 스케일 통일

**기대 효과:** 모델이 "절대 수익률"이 아니라 "위험 대비 방향성"을 학습 → 체제 전환 시 안정성 증가

---

### 4. Regime/체제 인식 메커니즘 부재

**현재 문제:** 2022년 약세장 데이터에 과적합 → 180일 예측 하향 편향 → 수동 보정이 필요했다. 모델이 "지금이 어떤 시장 국면인지" 명시적으로 모른다.

**개선안:**
```python
# Hidden Markov Model로 체제 분류
regime_labels = hmm_model.predict(volatility_series)  
# 0=저변동 상승, 1=고변동 하락, 2=횡보, 3=버블

# 방법 A: Regime을 피처로 추가
features['current_regime'] = regime_labels[-1]
features['regime_duration_days'] = count_since_last_switch

# 방법 B: Regime별 모델 분리 학습
model_bull.predict(features)  # 상승장 전용
model_bear.predict(features)  # 하락장 전용
```

**기대 효과:** 180일 보정 로직 불필요, 체제 전환 직후 예측 반응 속도 향상

---

### 5. 피처 171개의 차원 저주 & 다중공선성

**현재 문제:** 171개 피처 중 상당수가 강하게 상관된다.
- `ma7, ma14, ma30, ma50, ma90, ma200` → 서로 0.95+ 상관
- `log_btc_close`와 `btc_close_lag1` → 거의 동일
- `btc_vol_7d, 30d, 60d` → 높은 상관

**개선안:**
```
Step 1: VIF(분산팽창계수) > 10인 피처 제거
Step 2: Permutation Importance로 실제 기여도 측정
Step 3: 기간별로 중요 피처가 다를 수 있으므로 기간별 피처 선택
Step 4: PCA 또는 Autoencoder로 차원 축소 후 잠재 피처 생성
```

**목표:** 171개 → 40~60개 핵심 피처로 축소

**기대 효과:** 과적합 감소, 학습 안정성 향상, 해석력 증가

---

## 🟠 높은 우선순위 (모델 품질 직접 영향)

---

### 6. Transformer 아키텍처 정교화

**현재 문제:** `입력 선형변환 + Positional Encoding + Transformer Encoder + 마지막 시점 출력`은 가장 기본적인 구조다.

**개선안:**
- **Temporal Fusion Transformer (TFT)** 구조 도입: 정적 피처(halving_era, 이벤트 더미)와 동적 피처(가격, 거래량)를 분리 처리
- **Variable Selection Network**: 모델이 스스로 어떤 피처가 중요한지 attention으로 선택
- **Multi-horizon head**: 11개 기간을 하나의 모델에서 동시에 예측 (현재 기간별 독립 모델 → 기간 간 일관성 부여)
- **Decoder 추가**: Encoder-only → Encoder-Decoder로 확장하여 autoregressive 보완

---

### 7. 앙상블 전략 복원 및 고도화

**현재 문제:** xgboost, lightgbm, random_forest 흔적이 있지만 안 쓴다. Transformer-only는 단일 모델 리스크가 크다.

**개선안:**
```
Final_prediction = w1 × TimeSformer(features)
                 + w2 × LightGBM(features) 
                 + w3 × XGBoost(features)
                 + w4 × LSTM(features)

가중치 결정: 최근 N일 검증 성능의 역수 비례 (동적 가중치)
```

**기대 효과:** 단일 모델 과적합 위험 분산, 특히 체제 전환기에서 안정성 확보

---

### 8. 손실 함수 개선

**현재 문제:** `SmoothL1Loss`는 범용적이지만, 금융 시계열의 특성(비대칭 위험, 꼬리 리스크)을 반영하지 못한다.

**개선안:**
```python
# 비대칭 손실: 하락 예측 실패에 더 큰 페널티
class AsymmetricLoss(nn.Module):
    def forward(self, pred, actual):
        error = actual - pred
        loss = torch.where(error > 0, 
                          1.0 * error**2,   # 상승을 못 맞춤
                          2.0 * error**2)    # 하락을 못 맞춤 (더 위험)
        return loss.mean()

# 또는 Quantile Loss (10%, 25%, 50%, 75%, 90%)
# 또는 방향 정확도 보조 손실 추가
total_loss = SmoothL1Loss + λ * direction_accuracy_loss
```

---

### 9. 학습 데이터 양과 질의 불균형

**현재 문제:** BTC 일봉 데이터는 약 3,500~4,000일 정도. 60일 윈도우를 쓰면 학습 샘플 ~3,900개. 171개 피처 대비 샘플이 적다.

**개선안:**
- **데이터 증강:** 시계열 augmentation (jittering, scaling, window warping)
- **멀티타임프레임:** 일봉 외에 4시간봉, 주봉을 보조 학습에 활용
- **합성 데이터:** GAN 또는 변동성 모델(GARCH)로 유사 시나리오 생성
- **전이 학습:** 주식, 금, 원유 등 타 자산 시계열로 사전 학습 → BTC 미세 조정

---

### 10. 온체인 데이터의 극적 부족

**현재 문제:** 온체인은 해시레이트와 Fear & Greed 두 개뿐이다. BTC는 온체인 분석이 핵심인 자산이다.

**추가해야 할 온체인 피처:**
```
필수:
├── 거래소 순유입/유출량 (Exchange Netflow)
├── 장기보유자(LTH) 공급량 변화
├── 단기보유자(STH) 실현손익 비율 (SOPR)
├── MVRV Z-Score
├── NUPL (Net Unrealized Profit/Loss)
├── Puell Multiple
├── 활성 주소 수 (Active Addresses)
├── 스테이블코인 거래소 보유량 (구매력 proxy)
└── 고래 지갑 움직임 (>1000 BTC 이동)

데이터 소스: Glassnode API, CryptoQuant API
```

**기대 효과:** 가격에 선행하는 구조적 신호 포착 → 특히 중장기(30d+) 예측 정확도 개선

---

### 11. 이벤트 더미 변수의 경직성

**현재 문제:** 코로나, 루나, FTX 등을 하드코딩 더미 변수로 넣었다. 이는 과거 이벤트만 코딩된 것이고, **미래의 새로운 이벤트**에는 반응하지 못한다.

**개선안:**
```
방법 A: 이벤트 더미 제거 → 이벤트의 "결과"(변동성 급등, 상관관계 변화)가 
        이미 다른 피처에 반영되므로 중복

방법 B: NLP 기반 실시간 이벤트 감지
        ├── 뉴스 헤드라인 감성 분석 (GPT/FinBERT)
        ├── 트위터/레딧 감성 스코어
        ├── Google Trends "bitcoin" 검색량
        └── 이상치 탐지 → 자동 "충격 스코어" 생성

방법 C: 이벤트 카테고리화
        ├── regulatory_shock_score (규제 충격)
        ├── exchange_failure_score (거래소 사고)
        ├── macro_surprise_score (경제지표 서프라이즈)
        └── 각각 연속 변수로 표현
```

---

### 12. 예측 캘리브레이션 (Calibration) 부재

**현재 문제:** 모델이 "+15% 오를 것"이라고 해도, 그 예측이 역사적으로 얼마나 정확했는지 사용자가 모른다.

**개선안:**
```
1. 백테스트 결과를 예측 구간별로 집계
   - "30일 +10~20% 예측" → 실제 적중률 62%, 평균 실제 수익 +12%
   - "365일 +50%+ 예측" → 실제 적중률 23%, 평균 실제 수익 +31%

2. 예측값에 캘리브레이션 함수 적용
   calibrated_pred = isotonic_regression(raw_pred, historical_actual)

3. 대시보드에 "이 예측의 과거 정확도" 표시
```

---

## 🟡 중간 우선순위 (점진적 성능 향상)

---

### 13. Positional Encoding 개선

**현재 문제:** 표준 sinusoidal PE는 "순서"만 인코딩하고 "시간 간격"이나 "시간적 맥락"을 모른다.

**개선안:**
- **Calendar Encoding:** 요일, 월, 분기, 연초 효과, 옵션 만기 주기를 PE에 포함
- **Learnable PE:** 학습 가능한 위치 임베딩으로 교체
- **Relative PE (RPE):** 절대 위치 대신 상대 거리 기반 attention

---

### 14. 입력 윈도우 60일의 최적성 검증

**현재 문제:** 60일이 모든 예측 기간에 최적인지 검증 안 됨. 1일 예측에는 60일이 과하고, 365일 예측에는 부족할 수 있다.

**개선안:**
```
예측 기간    권장 입력 윈도우    이유
1일          20~30일           단기 모멘텀
7일          30~60일           중기 추세
30일         60~120일          사이클 패턴
90일         120~250일         반감기 효과
365일        250~500일         장기 매크로 사이클
```
- 기간별로 다른 윈도우 사용 또는 **Multi-scale 입력** (20일 + 60일 + 200일 동시 입력)

---

### 15. 스케일링 전략 정교화

**현재 문제:** 최근 60일 평균/표준편차로 스케일링 → 체제 전환 시 스케일이 급변해서 모델 입력이 불안정해진다.

**개선안:**
- **Robust Scaling:** median과 IQR 기반 (이상치에 강함)
- **전체 학습 데이터 통계 사용** (추론 시에도 고정 통계)
- **RevIN (Reversible Instance Normalization):** 모델 내부에서 정규화/역정규화 자동 처리
```python
# RevIN: 입력 시 정규화, 출력 시 역정규화
class RevIN(nn.Module):
    def forward(self, x, mode='norm'):
        if mode == 'norm':
            self.mean = x.mean(dim=1, keepdim=True)
            self.std = x.std(dim=1, keepdim=True)
            return (x - self.mean) / (self.std + 1e-5)
        else:  # denorm
            return x * self.std + self.mean
```

---

### 16. 금리/매크로 데이터의 발표 시차(Release Lag) 정합성

**현재 문제:** release lag 적용 언급은 있지만, 각 데이터의 실제 발표 지연이 정확한지 의문이다.

**검증 및 개선:**
```
데이터             실제 발표 지연    적용해야 할 lag
CPI               ~13일           최소 15일
GDP (속보)        ~30일           최소 30일  
실업률            ~5일            최소 7일
M2                ~3주            최소 21일
FOMC 결정         당일             0일 (하지만 점도표 해석은 며칠)
해시레이트         실시간           1일
Fear & Greed      실시간           1일
```
- 잘못된 lag → 미래 정보 누수 → 백테스트 과대평가

---

### 17. 선물/파생 피처의 실제 유효성

**현재 문제:** 선물 관련 피처(만기일까지 남은 일수, 스프레드 proxy, roll return)가 "proxy"로 표시되어 있다. 실제 BTC 선물 데이터가 아니라 commodity 선물을 쓰는 것으로 보인다.

**개선안:**
```
실제 BTC 파생 데이터 추가:
├── CME BTC 선물 베이시스 (연환산)
├── 선물 미결제약정(OI) 변화율
├── 선물 펀딩비율 (perpetual swap)
├── 옵션 25-delta skew
├── 옵션 내재변동성 (DVOL)
├── Put/Call 비율
└── 최대 고통가격 (Max Pain)

데이터 소스: Deribit API, Coinalyze, Laevitas
```

---

### 18. 기간 간 일관성 제약 (Cross-Horizon Consistency)

**현재 문제:** 11개 모델이 완전히 독립이므로, "7일 뒤 +5%, 30일 뒤 -3%, 60일 뒤 +8%"처럼 경제적으로 비합리적인 결과가 나올 수 있다. 보간(interpolation)은 빈 구간 채우기일 뿐 일관성 보장이 아니다.

**개선안:**
```python
# 방법 A: Multi-task 학습으로 하나의 모델이 모든 기간 동시 예측
class MultiHorizonModel(nn.Module):
    def forward(self, x):
        shared = self.encoder(x)
        return {
            '1d': self.head_1d(shared),
            '7d': self.head_7d(shared),
            '30d': self.head_30d(shared),
            # ...
        }

# 방법 B: 후처리 일관성 제약
# 7일 예측 ≤ 30일 예측 ≤ 90일 예측 (상승 추세 시)
# 누적 수익률 경로가 비점프(non-jump) 곡선을 형성하도록 정규화
```

---

### 19. 학습 하이퍼파라미터 최적화 자동화

**현재 문제:** `Adam(lr=0.0001)`이 고정이다. 기간별로, 데이터 양별로, 시장 국면별로 최적 하이퍼파라미터가 다르다.

**개선안:**
```
Optuna 또는 Ray Tune 적용:
├── 탐색 대상
│   ├── learning_rate: [1e-5, 1e-3]
│   ├── n_heads: [2, 4, 8]
│   ├── n_layers: [2, 4, 6]
│   ├── d_model: [64, 128, 256]
│   ├── dropout: [0.1, 0.3, 0.5]
│   ├── batch_size: [16, 32, 64]
│   └── window_size: [30, 60, 120]
├── 검증: Walk-Forward on last 2 years
└── 기간별 독립 최적화
```

---

## 🟢 보완적 개선 (안정성·운영)

---

### 20. 실시간 모델 성능 모니터링 & 자동 재학습

**현재 문제:** 한 번 학습된 모델이 계속 쓰인다. 시장 구조가 바뀌면 (예: 현물 ETF 출시 이후) 모델 성능이 저하되는데, 이를 감지하는 메커니즘이 없다.

**개선안:**
```
모니터링 대시보드:
├── 일일 예측 vs 실제 추적 (rolling MAE, 방향 정확도)
├── 피처 분포 이동 감지 (KS test, PSI)
├── 예측 잔차의 자기상관 검정 (잔차에 패턴 남으면 모델 열화)
└── 임계치 초과 시 자동 재학습 트리거

재학습 전략:
├── 증분 학습 (최근 데이터만 fine-tuning)
├── 전체 재학습 (월 1회)
└── 체제 전환 감지 시 즉시 재학습
```

---

### 21. 백테스트 프레임워크 엄밀화

**현재 문제:** 백테스트가 있지만, 얼마나 엄밀한지 불명확하다.

**개선안:**
```
필수 백테스트 지표:
├── 기간별 방향 정확도 (Direction Accuracy)
├── MAE, RMSE, MAPE (기간별)
├── 수익률 예측의 IC (Information Coefficient)
├── 최대 연속 실패 횟수
├── 시장 국면별 성능 분해
│   ├── 상승장 정확도
│   ├── 하락장 정확도
│   └── 횡보장 정확도
├── 편향 분석: 평균적으로 상승/하락 어디로 치우치나
└── 경제적 가치 테스트: 예측 기반 단순 전략의 Sharpe ratio
```

---

### 22. KRW 환율 변환의 예측 오차 전파

**현재 문제:** 모델은 USD 기준 BTC를 예측하고, 대시보드에서 현재 원달러 환율을 곱해 KRW로 보여준다. 하지만 30일 뒤, 180일 뒤의 환율은 지금과 다를 수 있다.

**개선안:**
```
방법 A: 원달러 환율도 별도 예측 모델 구축
        KRW_future = predict_krw(macro_features)
        BTC_KRW_future = BTC_USD_future × KRW_future

방법 B: 환율 시나리오 제시
        ├── 환율 변동 없음: ₩120,000,000
        ├── 원화 약세 5%:   ₩126,000,000  
        └── 원화 강세 5%:   ₩114,000,000

방법 C: 환율 불확실성을 오차 범위에 포함
        USD 예측 오차 ± 환율 변동 분산 = 총 KRW 예측 불확실성
```

---

## 요약 우선순위 매트릭스

```
순위  개선 포인트                        난이도   영향도   긴급도
─────────────────────────────────────────────────────────────
 1   확률분포 예측 전환                   ★★★★   ★★★★★  ★★★★★
 2   학습/검증 분할 전략 (Walk-Forward)   ★★★    ★★★★★  ★★★★★
 3   Target 설계 (변동성 정규화)          ★★     ★★★★★  ★★★★
 4   Regime 인식 메커니즘                 ★★★★   ★★★★★  ★★★★
 5   피처 차원 축소 & 선택               ★★★    ★★★★   ★★★★
 6   Transformer 아키텍처 (TFT 등)       ★★★★   ★★★★   ★★★★
 7   앙상블 전략 복원                     ★★★    ★★★★   ★★★
 8   손실 함수 개선                       ★★     ★★★★   ★★★
 9   학습 데이터 증강                     ★★★    ★★★    ★★★
10   온체인 데이터 확충                   ★★★    ★★★★   ★★★
11   이벤트 더미 → 동적 감지             ★★★★   ★★★    ★★★
12   예측 캘리브레이션                    ★★★    ★★★★   ★★★
13   Positional Encoding 개선            ★★     ★★★    ★★
14   입력 윈도우 기간별 최적화            ★★     ★★★    ★★
15   스케일링 전략 (RevIN)               ★★     ★★★    ★★
16   Release Lag 정합성 검증             ★★     ★★★    ★★★
17   실제 BTC 파생 데이터 추가           ★★★    ★★★    ★★
18   기간 간 일관성 제약                  ★★★    ★★★    ★★
19   하이퍼파라미터 자동 최적화           ★★★    ★★★    ★★
20   실시간 모니터링 & 자동 재학습        ★★★★   ★★★    ★★
21   백테스트 엄밀화                      ★★★    ★★★    ★★
22   KRW 환율 예측 오차 처리             ★★★    ★★     ★★
```

> **핵심 메시지:** 1~5번을 먼저 해결하면 나머지 개선의 효과가 배가된다. 특히 **확률 분포 예측 전환(1번)**과 **Walk-Forward 검증(2번)**은 다른 모든 개선의 평가 기반이 되므로 최우선이다.